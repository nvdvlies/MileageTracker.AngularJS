(function () {
	'use strict';

	describe('Unit: EditTripController', function() {
		var scope;
		
		beforeEach(function(){
			module('ngRoute');
			module('ngCookies');
			module('ui.bootstrap');
			module('ngTable');
			module('MileageTracker');
		});

		//should have cars after init

		//should have addresses after init

		//should have trip template after init

		//should save a trip

		//should redirect to /trips on cancel

		//should have error on invalid entry  

	});

}());